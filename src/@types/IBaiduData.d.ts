declare module '@giligili'{
  interface IBaiduData{
    all: {
      avg: number
      yoy:number
      qoq:number
    },
    name: string
    sex:{
      F: string
      M: string
    },
    wise: {
      avg: number
      yoy:number
      qoq:number
    },
    pc: {
      avg: number
      yoy:number
      qoq:number
    },
    _id: string
  }
}
