declare module '@giligili' {
  interface ITGBus {
    _id: string
    name: string
    subname: string
    coverUrl: string
    introduction: string
    startDate: string
    issuer: string
    engine: string
    type: string[]
    theme: string[]
    mode: string[]
    view: string[]
    description: string
    imgUrl: string[]
    avgScore: number
  }
}
